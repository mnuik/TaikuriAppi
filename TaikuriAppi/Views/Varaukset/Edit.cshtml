@model TaikuriAppi.Models.Varaukset

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>Varaukset</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="VarausId" />
            <div class="form-group">
                <label asp-for="Päivämäärä" class="control-label"></label>
                <input asp-for="Päivämäärä" class="form-control" />
                <span asp-validation-for="Päivämäärä" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="TaikuriId" class="control-label"></label>
                <select asp-for="TaikuriId" class="form-control" asp-items="ViewBag.TaikuriId"></select>
                <span asp-validation-for="TaikuriId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="AsiakasId" class="control-label"></label>
                <select asp-for="AsiakasId" class="form-control" asp-items="ViewBag.AsiakasId"></select>
                <span asp-validation-for="AsiakasId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
